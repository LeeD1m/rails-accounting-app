<div class="card bg-dark rounded-0 justify-content-center">
  <div class="card-header d-flex justify-content-between">
    <%= link_to root_path, class: 'navbar-brand text-white text-xl-left' do %>
      <div class="d-inline m-2">
        Accounting app
      </div>
    <% end %>
     <% unless !current_user %>
        <%= button_to 'Logout', logout_path, method: :delete, class: 'btn btn-danger' %>
     <% end %>
  </div>
</div>
